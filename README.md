This repository was created for my CS1103 Final Project.
To run the Java code. Compile in the Terminal using: javac LibraryDataInserter.java
And then use this command on the terminal line. 
java --enable-native-access=ALL-UNNAMED -classpath ".:sqlite-jdbc.jar:slf4j-simple.jar:slf4j-api.jar" LibraryDataInserter
